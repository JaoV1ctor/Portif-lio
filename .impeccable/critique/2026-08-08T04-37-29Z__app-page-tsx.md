---
target: homepage (app/page.tsx)
total_score: 29
max_score: 36
na_heuristics: 10
p0_count: 0
p1_count: 0
timestamp: 2026-08-08T04-37-29Z
slug: app-page-tsx
---
Method: dual-agent (A: a94817333b9fe0ed1 · B: a71248c1c5a8064ff)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Status badge, contador, skeletons, spinner, scroll bar |
| 2 | Match System / Real World | 3 | "vibe code" no Hero é jargão informal para público recrutador |
| 3 | User Control and Freedom | 3 | Sem Escape/clique-fora pra fechar estado de erro do form |
| 4 | Consistency and Standards | 3 | Amarelo residual em estrelas/badge de linguagem JS — categórico, não de estado; regra precisa clareza |
| 5 | Error Prevention | 3 | Validação em tempo real no Contact |
| 6 | Recognition Rather Than Recall | 4 | Filtros visíveis, Terminal com `help` descobrível |
| 7 | Flexibility and Efficiency | 3 | Terminal com histórico via seta cima/baixo |
| 8 | Aesthetic and Minimalist Design | 3 | Hero ainda denso mas aceitável pra landing de impacto |
| 9 | Error Recovery | 3 | Mensagens específicas, retry claro (sem regressão) |
| 10 | Help and Documentation | n/a | superfície Experience/Persuade |
| **Total** | | **29/36** | **Good (81%)** |

## Design Specificity Verdict

**LLM assessment**: Específico e ancorado em código real. Sistema "Command Deck" coerente e consistentemente aplicado. Issues remanescentes são polimento, não falhas estruturais.

**Deterministic scan**: 3 achados (caiu de 16) — 1 `layout-transition` (`globals.css:57`), 2 `gradient-text` (decorativos legítimos em heading/métrica, não falsos positivos). **Os 13 achados de `design-system-font-size` sumiram por completo** — o token `--text-2xs` consolidou todos os valores soltos dentro do type ramp reconhecido pelo detector.

**Visual overlays**: indisponíveis — sem Playwright/Puppeteer nesta sessão.

## Confirmação das correções desta sessão

1. **Badge duplicado** (`ProjectCard.tsx:144`) — confirmado resolvido.
2. **Debounce de resize** (`Projects.tsx:118-130`) — confirmado resolvido. Nota: `checkScroll` (linha 82) ficou sem o mesmo debounce — baixo risco, ver P3.
3. **Token `text-2xs`** — confirmado resolvido, aplicado ponta a ponta nos 9 arquivos, detector zerou a regra.
4. **Toggle 3D do Hero** — confirmado resolvido, ícone-only 44×44 claramente secundário.

## Overall Impression

Ciclo fechado com sucesso: score subiu de 21 → 23 → 29 (numa base agora maior, 36 em vez de 32, porque a heurística 7 passou a ser pontuada). Nenhum P0/P1 restante. O que sobra é polimento fino e uma pergunta de interpretação de regra (amarelo categórico vs. amarelo de estado).

## What's Working

- Priorização de recência + hierarquia de evidência: aba "Professional" abre por padrão, tudo ordenado por data.
- Acessibilidade de teclado no TechStack via `focus-within`/`focus-visible` — tooltip acessível sem mouse, detalhe raro e bem cuidado.
- Token `text-2xs` aplicado sem sobras em todos os 9 arquivos-alvo.

## Priority Issues

**[P2] Amarelo categórico (estrelas GitHub, badge linguagem JS) não é coberto pela Signal Rule como escrita** — `ProjectCard.tsx:33,111`. A regra do DESIGN.md diz "cor só sinaliza estado". Estrelas/linguagem são categóricas, não estado do sistema — zona cinzenta. Não corrigido agora porque a resposta certa depende de decisão de produto, não de bug. → decisão do usuário, depois `$impeccable colorize` se optar por restringir.

**[P3] `checkScroll` sem debounce** — `Projects.tsx:82`, listener de resize do indicador de scroll do filtro não tem o mesmo debounce aplicado ao `itemsPerPage`. Cálculo leve, risco baixo.

**[P3] Terminal.tsx:277 "joaovictor@matrix" hardcoded**, sem i18n — quebra o padrão usado no resto do componente.

**[P3] `Hero.tsx:15` `isMobileDevice` default `true`** — nota de código, sem flash de layout perceptível (SSR já não renderiza o toggle nesse estado).

## Persona Red Flags

Nenhum red flag bloqueante para Jordan ou Alex nesta rodada — hierarquia clara, prova técnica visível cedo, badge "Client Work" + resultMetric é o gancho de ROI que Alex busca.

## Minor Observations

- `page.tsx:2,13` comentários residuais de debug ("Forçando re-renderização...", "Cache removido...") — ruído de código, não afeta design.
- `Contact.tsx:60` expõe endpoint FormSubmit com e-mail em texto puro no bundle client — fora de escopo de design, mas visível no código-fonte.

## Questions to Consider

- A Signal Rule vale só pra cor de estado do sistema (sucesso/erro/loading) ou também pra cor categórica informativa (estrelas, linguagem)? Isso decide se `ProjectCard.tsx:33,111` precisa de ajuste.
- "vibe code" no Hero é diferencial de marca pessoal intencional, ou vale suavizar pro público recrutador mais corporativo?
